<!DOCTYPE html>
<html>
  <head>
    <title>AirDesk - Office Desk for Rent - No Commission</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap.css">
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/bootstrap/3.0.3/css/bootstrap-theme.css">
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <nav class="navbar navbar-default navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">AirDesk</a>
        </div>
    <div class="collapse navbar-collapse">
      <ul class="nav navbar-nav">
        <li ><a href="/rentals">Find a Desk</a></li>


        <p class="navbar-text pull-right">
          <% if admin_signed_in? %>
          Logged in as <strong><%= current_admin.email %></strong>.
          <%= link_to 'Edit profile', edit_admin_registration_path, :class => 'navbar-link' %> |
          <%= link_to "Logout", destroy_admin_session_path, method: :delete, :class => 'navbar-link'  %>
            <% elsif buyer_signed_in? %>
            Logged in as <strong><%= current_buyer.email %></strong>.
            <%= link_to 'Edit profile', edit_buyer_registration_path, :class => 'navbar-link' %> |
            <%= link_to "Logout", destroy_buyer_session_path, method: :delete, :class => 'navbar-link'  %>
            <% elsif owner_signed_in? %>
            Logged in as <strong><%= current_owner.email %></strong>.
            <%= link_to 'Edit profile', edit_owner_registration_path, :class => 'navbar-link' %> |
            <%= link_to "Logout", destroy_owner_session_path, method: :delete, :class => 'navbar-link'  %>
            <% else %>
            Become a Member
            <%= link_to "Sign up", new_buyer_registration_path, :class => 'navbar-link'  %> |
            <%= link_to "Login", new_buyer_session_path, :class => 'navbar-link'  %>
            Rent a Desk
            <%= link_to "Sign up", new_owner_registration_path, :class => 'navbar-link'  %> |
            <%= link_to "Login", new_owner_session_path, :class => 'navbar-link'  %>
    <% end %>
</p>
      </ul>
    </div>
  </div>
</nav>
    <div class="container">
      <% if notice %>
      <p class="alert alert-success"><%= notice %></p>
      <% end %>
      <% if alert %>
      <p class="alert alert-danger"><%= alert %></p>
      <% end %>


      <%= yield %>
    </div>
    <footer>
  <div class="container">
  By<a target=”_blank” href="https://quentinguillaume.com">  Couentine <%= Time.now.year %> </a>
  </div>
</footer>
<script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.4/jquery.min.js"></script>
<script src="//maxcdn.bootstrapcdn.com/bootstrap/3.0.3/js/bootstrap.js"></script>
  </body>
</html>
